"""Flask blueprints for the Inventory Tracker (incremental refactor of the
app.py monolith — see REFACTOR_PLAN.md). Blueprint modules must NOT import
app.py; shared helpers move to a core module as the refactor proceeds."""
